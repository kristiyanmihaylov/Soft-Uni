function multiplication(num) {
    //(number)X(times)=(product)
    for (let i = 1; i <= +10; i++) { //kolko puti
        console.log(`${num} X ${i} = ${num*i}`);
    } // napravo v producta napravih operaciq 
}
multiplication(5);